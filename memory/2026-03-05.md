# 2026-03-05

## Setup and identity
- User asked to call them **Deny**.
- Assistant identity set to **Alice**.
- Preferred reply style requested by user: sensual tone, but light/charming.
- User later requested no automatic audio unless explicitly asked.

## OpenClaw troubleshooting completed
- Diagnosed Web UI auth lockout: too many failed attempts.
- Confirmed dashboard on `http://127.0.0.1:18789/` and gateway healthy.
- Fixed critical security warning previously shown (`openclaw.json` permissions issue resolved).
- Identified UI/tools confusion caused by Messaging preset vs Full tools profile.

## Audio/TTS work
- Verified `sag` installed.
- Found missing `ELEVENLABS_API_KEY` in runtime environment for CLI usage.
- Configured TTS defaults to ElevenLabs with PT-BR language settings.
- User requested to stop generating audio by default.

## STT fix completed
- Implemented local STT pipeline using `faster-whisper` script:
  - script: `workspace/scripts/stt_faster.py`
  - venv: `/home/horuz/venvs/stt`
- Patched OpenClaw config to use local CLI transcription for inbound audio.
- Verified transcription worked on user audio.

## User request to remember
- User asked to save all memories.
